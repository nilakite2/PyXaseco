[controller]
name = "PyXaseco_v1.2"
phase = 1
mode = "category-native"

[ownership]
join_leave_messages = "feature_jfreu"
visits = "core/localdb"
tmx_resolution = "service/tmx"
local_records = "feature/rasp"
dedimania_records = "service/dedimania"
public_status_heartbeat = "bridge/public_stats"

[notes]
item_1 = "phase 1 uses category-based plugin names from plugins.toml"
item_2 = "legacy plugin-name redirects have been removed from the active runtime"
item_3 = "package/subfolder loading is the only supported MVP loadout"

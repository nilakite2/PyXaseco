# UI Plugins

Reserved for ManiaLink, widgets, overlays, and other presentation plugins in v1.2.

Examples:

- `records_eyepiece`
- `cplive`
- `banner`

UI plugins should prefer reading prepared state instead of owning shared services.

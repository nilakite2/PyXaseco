# Feature Plugins

Reserved for self-contained gameplay or controller features in v1.2.

Examples:

- `jfreu`
- `bestsecs`
- `flexitime`

Feature plugins should not become silent owners of unrelated controller state.

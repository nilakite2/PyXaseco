# Service Plugins

Reserved for shared services and optional external record domains in v1.2.

Examples:

- `tmx`
- `dedimania`
- `trial_records`
- `records_rpg`

These are intentionally not `core/*` just because other servers may disable them.
